/**
 * {{pkg.descriptoin}}
 * @mdoule {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const fmtjson = require('./fmtjson')

let lib = fmtjson.bind(this)
{{#each sub}}
lib.{{this}} = fmtjson.{{this}}
{{/each}}

module.exports = lib