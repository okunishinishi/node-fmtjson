/**
 * {{pkg.descriptoin}}
 * @mdoule {{pkg.name}}
 * @version {{pkg.version}}
 */

"use strict";

var fmtjson = require('./fmtjson'),
    pkg = require('../package.json');

var lib = fmtjson.bind(this);
lib.version = pkg.version;

module.exports = lib;