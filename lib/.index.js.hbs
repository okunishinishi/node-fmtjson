/**
 * {{pkg.descriptoin}}
 * @mdoule {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const fmtjson = require('./fmtjson')
const pkg = require('../package.json')

let lib = fmtjson.bind(this)
lib.version = pkg.version
{{#each sub}}
lib.{{this}} = fmtjson.{{this}}
{{/each}}

module.exports = lib